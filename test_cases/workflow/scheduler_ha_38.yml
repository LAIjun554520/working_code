# 需要创建的任务流的数量及分布（均分）
taskflow_num: 30
dir_name: "scheduler_ha_38"
dir_num: 1

# 需要创建的任务流的基本信息及数量，暂无支持任务流之间的依赖关系，暂不支持任务之间定义依赖关系；目前仅支持脚本任务
taskflow_message:
  - num: 30
    name_pattern: "scheduler_ha_38"
    cron_conf:
      cron_type: "CRON_EXPRESSION"
      cron_Pattern: "0 9 18 ? * * *"
      cron_Hour: 0
      cron_Minute: 0
    dependLastPolicy: "NO"
    schedulePriority: "LOW_PRIORITY"
    executorGroupId: -1
    pattern_file: "scheduler_ha_38.json"
    task_message: " "